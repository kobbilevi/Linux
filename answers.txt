# Keyboard layout configuration
KEYMAPOPTS="us us"

# Interactive prompt for hostname only
HOSTNAMEOPTS="none"

# Use the lightweight, modern device manager
DEVDOPTS=mdevd

# Network interface configuration
INTERFACESOPTS="auto lo
iface lo inet loopback

auto eth0
iface eth0 inet dhcp
"

# Inherit DNS configurations dynamically via DHCP
DNSOPTS=none

# Timezone configuration
TIMEZONEOPTS="Asia/Jerusalem"

# Proxy configuration
PROXYOPTS=none

# Official Alpine Linux CDN Mirror URL
APKREPOSOPTS="http://dl-cdn.alpinelinux.org/alpine/"

# Create the deployment user dynamically at runtime
USEROPTS="TARGET_USER_PLACEHOLDER"
USERSSHKEY="https://raw.githubusercontent.com/kobbilevi/Linux/refs/heads/main/SSHKey.pub"

# Handle SSH server manually via execution script for granular control
SSHDOPTS=none
ROOTSSHKEY=none

# Modern and accurate NTP client
NTPOPTS=chrony

# Disk target is dynamically injected by the wrapper script at runtime
DISKOPTS="TARGET_DISK_PLACEHOLDER"

# Disable local cache/backup as we run directly off the physical disk
LBUOPTS=none
APKCACHEOPTS=none
